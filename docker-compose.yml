services:
  redis:
    image: redis:7
    container_name: ratelimiter-redis
    ports:
      - "6379:6379"

  ratelimiter:
    build: .
    container_name: ratelimiter-app
    ports:
      - "8080:8080"
    environment:
      REDIS_URL: redis://redis:6379
    depends_on:
      - redis